name: Rubocop

on:
  pull_request:
  push:
    branches:
      - 'master'
      - 'develop'

jobs:
  build:
    name: Run Rubocop Linting
    runs-on: ubuntu-latest
    env:
      api-dir: ./
    steps:
      - uses: actions/checkout@v2
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: 2.5.9
          bundler-cache: true
      - name: Install Rubocop
        working-directory: ${{env.api-dir}}
        run: gem install rubocop-ast:1.17.0 rubocop:1.28
      - name: Check Rubocop Styles
        working-directory: ${{env.api-dir}}
        run: rubocop
