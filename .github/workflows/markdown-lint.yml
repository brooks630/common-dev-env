name: Rubocop

on:
  pull_request:
  push:
    branches:
      - 'master'
      - 'develop'

jobs:
  build:
    name: Run Rubocop Linting
    runs-on: ubuntu-latest
    env:
      api-dir: ./
    steps:
      - uses: articulate/actions-markdownlint@v1
